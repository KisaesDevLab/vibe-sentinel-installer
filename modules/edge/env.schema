# modules/edge/env.schema — KEY=description:required|optional
SENTINEL_MODULES_DIR=Staged module directory (bouncer config mount):required
CROWDSEC_BOUNCER_KEY_FIREWALL=LAPI key for the nftables bouncer; injected on first boot:optional
