# mesh — migrations

Placeholder. NetBird management-store migrations between pinned versions land
here as ordered `NNN-*.sh` steps. NetBird is harness-gated: upgrade.sh
refuses to move it to a version whose manifest entry lacks
`harness_passed: true`.
