# modules/mesh/env.schema — KEY=description:required|optional
SENTINEL_DOMAIN=Firm domain; nb./nb-signal./nb-admin. hostnames hang under it:required
SENTINEL_MODULES_DIR=Staged module directory (management.json mount):required
NETBIRD_RELAY_SECRET=Relay auth secret; needed only when the opt-in relay sub-module is enabled:optional
