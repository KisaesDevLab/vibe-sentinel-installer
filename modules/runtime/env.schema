# modules/runtime/env.schema — KEY=description:required|optional
SENTINEL_MODULES_DIR=Staged module directory (rules.d mount):required
FALCO_PRIVILEGED_FALLBACK=true when kernel preflight found no BTF; documents the privileged fallback:optional
